let dom = `<form action="#">`+
`<fieldset>`+
`<legend>1번 범주</legend>` +
`<input type="text">` +
`</fieldset>` +
`<fieldset>` +
`<legend>2번 범주</legend>` +
`<input type="text">` +
`</fieldset>` +
`</form>`;

// document.write(dom);

// 백틱으로 편하게 dom 을 그릴 수 있다.
let dom2 = `<form action="#">
<fieldset>
    <legend>1번 범주</legend>
    <input type="text">
</fieldset>
<fieldset>
    <legend>2번 범주</legend>
    <input type="text">
</fieldset>
</form>`

document.write(dom2)